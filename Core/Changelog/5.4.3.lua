local TXUI, F, E, I, V, P, G = unpack(select(2, ...))

TXUI.Changelog["5.4.3"] = {
    -- LuaFormatter off
    HOTFIX = true,
    CHANGES = {
        "* Misc",
        " Update " .. F.StringElvUI("ToxiUI") .. " for patch 9.2.5"
    }
    -- LuaFormatter on
}
