local TXUI, F, E, I, V, P, G = unpack(select(2, ...))

TXUI.Changelog["5.3.9"] = {
    -- LuaFormatter off
    HOTFIX = true,
    CHANGES = {
        "* Misc",
        " Fix previously added " .. F.StringElvUI("Broker Translocation Matrix") .. " hearthstone",
    }
    -- LuaFormatter on
}
