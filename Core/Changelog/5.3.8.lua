local TXUI, F, E, I, V, P, G = unpack(select(2, ...))

TXUI.Changelog["5.3.8"] = {
    -- LuaFormatter off
    HOTFIX = true,
    CHANGES = {
        "* Misc",
        " Add " .. F.StringElvUI("Broker Translocation Matrix") .. " to WunderBar Hearthstone list",
    }
    -- LuaFormatter on
}
