local TXUI, F, E, I, V, P, G = unpack(select(2, ...))

TXUI.Changelog["5.3.7"] = {
    -- LuaFormatter off
    HOTFIX = true,
    CHANGES = {
        "* General",
        "Version bump for patch 9.2.0",	
		"set ElvUI Version to min 12.65",	
	
        "* Hotfix",
        "fixed Issue with Requirements for ToxiUI Vehicle Bar and SLE Vehiclebar is acctivated",
    }
    -- LuaFormatter on
}
