local TXUI, F, E, I, V, P, G = unpack(select(2, ...))

TXUI.Changelog["5.1.1"] = {
    -- LuaFormatter off
    HOTFIX = true,
    CHANGES = {
        "* Misc",
        " TOC Update for 9.0.5"
    }
    -- LuaFormatter on
}
