local TXUI, F, E, I, V, P, G = unpack(select(2, ...))

TXUI.Changelog["4.5.2"] = {
    -- LuaFormatter off
    HOTFIX = true,
    CHANGES = {
        "* ElvUI",
        " Fixed Action Bars size",
    }
    -- LuaFormatter on
}
